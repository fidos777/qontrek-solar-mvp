{
  "name": "qontrek-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev": "npm run dev --workspace=apps/solar-demo",
    "build": "npm run build --workspaces",
    "build:packages": "npm run build --workspace=packages/core && npm run build --workspace=packages/civos && npm run build --workspace=packages/solar-worker",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
